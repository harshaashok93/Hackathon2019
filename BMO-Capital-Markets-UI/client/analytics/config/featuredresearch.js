export const config = {
  menuClick: { // Whenever we click a specific overview link
    trigger: 'click',
    type: 'event',
    category: 'Featured & Recommended Research',
    action: 'Overview'
  }
};
